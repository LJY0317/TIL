## front/feature-21 관련 (구글 소셜 로그인)
2026. 01. 28.
**📨 백엔드 개발자에게 전달할 수정 요청서**

**1. 로그인 응답(Response)에 '유저 정보'를 포함해 주세요. (가장 중요 ⭐)**

현재 상황: 로그인 API(POST /auth/google)가 달랑 accessToken 하나만 줍니다.
문제점: 프론트엔드가 정보를 얻기 위해 GET /users/me를 한 번 더 호출해야 합니다. (네트워크 낭비 + 로딩 시간 증가)
요청사항: 로그인 성공 시, 토큰과 함께 유저 정보(User DTO) 도 한 번에 내려주세요.
```json
// 이렇게 바꿔주세요!
{
  "accessToken": "eyJh...",
    "isNewUser": true
    "isNotificationEnabled": true
  }
}
```

**2. isNewUser (신규 가입 여부) 필드를 명시해 주세요.**

현재 상황: 프론트엔드가 "키워드가 비어있으면 신규 유저겠지?"라고 추측하고 있습니다.
문제점: 만약 기존 유저가 키워드를 다 지운다면? 프론트엔드는 그 사람을 신규 유저로 착각하고 가입 화면으로 보낼 위험이 있습니다.
요청사항: 백엔드에서 created_at을 확인하거나 가입 로직을 탈 때 isNewUser: true를 확실하게 내려주세요.

**3. LazyInitializationException 방지 (OSIV 이슈)**

현재 상황: 
User
 엔티티 안에 keywords(@ElementCollection)가 있는데, 컨트롤러에서 이걸 조회하려다 500 에러가 났습니다. (DB 세션이 먼저 끊김)
임시 조치: 제가 프론트엔드 로컬 설정에서 open-in-view: true로 강제해서 막았습니다.
요청사항: 
Service
 단에서 Transactional 안에서 확실하게 DTO로 변환해서 나오거나, JOIN FETCH를 써서 한 번에 가져오도록 쿼리를 튜닝해 주세요. (프론트 설정에 의존하면 안 됩니다!)


이 3가지만 고쳐지면 프론트엔드 코드가 절반 이하로 줄어들고, 에러 날 확률도 0% 에 수렴하게 됩니다. 꼭 전달해서 더 편한 개발 환경 만드세요! 화이팅입니다! 💪🔥🔥


---
2026. 01. 29. 13:30
**backend/develop 코드 확인 결과: 완벽합니다! 💯**

백엔드 개발자님이 요청사항 3가지를 모두 정확하게 반영해주셨습니다.

1. 로그인 응답에 '유저 정보' 포함 (⭐)
AuthController.java에서 LoginResponse 생성 시 UserInfo를 함께 담아서 보내줍니다.
이제 프론트에서 /users/me를 따로 호출할 필요가 없어졌습니다! (네트워크 요청 1회 절약)
2. isNewUser 필드 명시
UserService.loginOrSignup에서 회원가입 시 true, 로그인 시 false를 명확하게 구분하여 리턴하고 있습니다.
더 이상 키워드 개수로 신규 유저를 추측하지 않아도 됩니다.
3. LazyInitializationException 방지
UserService.java를 보니 userRepository.findByProviderAndProviderIdWithkeywords(...) 라는 메서드를 쓰고 있습니다.
이름만 봐도 **"키워드까지 한 번에(Join Fetch) 가져오겠다"**는 의지가 보입니다. 이제 500 에러 걱정 끝입니다.
**결론**: 백엔드 코드가 아주 훌륭하게 개선되었습니다. 프론트엔드 로그인 로직을 안심하고 대폭 간소화하셔도 됩니다! 🚀



## front/feature-22 관련 (로그인 성공 후 관심 키워드 입력)
2026. 01. 30.
Toast 알림 기능 구현 완료 (라이브러리 X, 직접 구현 O)
이번에 알림 메시지를 띄우는 Toast(토스트) 기능을 구현했습니다. 무거운 외부 라이브러리(react-toastify 등)를 쓰지 않고 Zustand로 직접 가볍게 만들어서 성능도 좋고 커스터마이징도 편해요.

💡 사용 방법 어디서든 useToastStore만 불러오면 바로 쓸 수 있습니다.
```
import { useToastStore } from '@/store/useToastStore';

const MyComponent = () => {
  const { addToast } = useToastStore();

  const handleClick = () => {
    // 사용법: addToast('메시지', '타입')
    addToast('로그인 성공!', 'success'); // 초록색 체크 ✅
    addToast('에러가 발생했습니다.', 'error'); // 빨간색 경고 🚨
    addToast('잠시만 기다려주세요.', 'info'); // 파란색 정보 ℹ️
  };
};
```
전역 관리: Zustand로 관리해서 모달 위든, 페이지 어디든 최상단에 잘 뜹니다.

🧐 왜 지금 방식이 더 좋을까요?
1. 이미 Zustand를 쓰고 계시니까요!
  만약 Zustand가 없었다면 Context API 등을 새로 짜야 해서 번거로웠겠지만, 이미 전역 상태 관리를 쓰고 계시니 코드 몇 줄만 추가하면 공짜로 얻는 기능입니다.
2. 디자인 일관성 (shadcn/ui + Tailwind)
  외부 라이브러리는 독자적인 스타일을 가지고 있어서, 우리 앱의 디자인(폰트, 색상, 둥글기 등)과 미묘하게 안 맞을 수 있습니다. 직접 만들면 className으로 완벽하게 깔맞춤할 수 있죠.
3. 학습 효과 (SSAFY 프로젝트)
  "라이브러리 갖다 썼어요"보다 **"상태 관리 라이브러리를 활용해 전역 알림 시스템을 직접 설계했습니다"**가 포트폴리오 면접에서 훨씬 좋은 어필 포인트가 됩니다.

💡 언제 외부 라이브러리를 쓰면 좋을까요?
- 프로젝트 마감이 코앞이라 1초라도 아껴야 할 때.
"드래그로 닫기", "중복 방지", "최대 개수 제한", "프로미스 연동(로딩→성공 자동전환)" 등 아주 복잡하고 고급진 기능이 당장 필요할 때.






## front/feature-24 관련 (스터디 메인 페이지)
2026. 01. 29.
1. RightSidebarStudyMember.tsx 생성 완료:
    기존 사이드바 코드를 이 컴포넌트로 분리하여 저장했습니다.
    색상도 #171a1f 같은 하드코딩된 값 대신 bg-card, text-foreground, bg-primary, border-border 등 프로젝트 테마 변수로 리팩토링했습니다
    > 이제 RightSidebarStudyMember 컴포넌트는 components 폴더에 안전하게 보관되어 있으며, 나중에 버튼을 만들 때 언제든 가져와서 사용하시면 됩니다.


---
2026.01.29. 09: 44
**분석 결과 두 가지 문제의 원인을 찾아서 해결했습니다!** 🛠️

1. 토글 버튼이 안 되던 이유:
    - RightSidebarStudyMember 컴포넌트에 hidden xl:flex라는 클래스가 남아있어서, 화면이 작으면 토글 상태와 상관없이 강제로 숨겨지고 있었습니다.
    - 해결: 이 클래스를 제거하여, 이제 버튼을 누르면 화면 크기와 상관없이 사이드바가 정상적으로 나타납니다.

2. 이중 스크롤바가 생기던 이유:
    - StudyDetailPage가 h-[100vh-64px]로 화면 전체 높이를 강제로 차지하게 설정되어 있었는데, 이미 부모 레이아웃(Layout)이 패딩(p-6)을 가지고 있어서 내용물이 넘치게 되었습니다 (그래서 부모 스크롤 + 자식 스크롤 2개가 생성됨).
    - 해결: 높이를 h-full로 변경하여 부모 레이아웃의 크기에 딱 맞추도록 수정했습니다. 이제 깔끔하게 하나의 스크롤바만(혹은 영역별 스크롤) 나올 겁니다.


---
2026. 01. 29.
백엔드 개발자와 협업하여 실제 데이터를 화면에 뿌려주는 과정은 크게 **3단계(약속하기 -> 요청하기 -> 그리기)**로 나뉩니다. 텍스트로 쉽게 설명해 드릴게요! 👨‍🏫

1단계: 백엔드 개발자와 "약속(API 설계)" 하기 🤝
가장 먼저 백엔드 개발자에게 "어떤 주소로 요청하면, 어떤 모양의 데이터를 줄 거야?" 라고 물어보고 정해야 합니다.

요청 주소(URL): 예) GET /api/v1/studies/{studyId}/schedules
필요한 데이터: "A 스터디(ID: 1)의 일정을 줘!"
받을 데이터 모양(JSON):
```json
[
  { "id": 1, "date": "2024-02-01", "content": "1회차 모임: 오티" },
  { "id": 2, "date": "2024-02-08", "content": "2회차 모임: 알고리즘 풀이" }
]
```
(이런 식으로 약속을 정해두는 문서가 바로 **API 명세서**입니다.)


---
2026. 01. 29.
lucide-react에 대한 궁금증을 풀어드릴게요! 🧐

1. import { Search ... }가 뭔가요?
이 코드는 **"아이콘 꾸러미(lucide-react)에서 돋보기 모양(Search)과 오른쪽 화살표(ChevronRight)를 꺼내 쓰겠다"**는 의미입니다. 마치 레고 상자에서 내가 원하는 블럭 2개만 딱 집어내는 것과 같습니다.

2. 이모지 대신 쓰는 건가요?
네, 맞습니다! 하지만 단순 대체 그 이상입니다. 🎨

이모지(😀): 폰트마다(윈도우/맥/안드로이드) 모양이 제각각이고, 색상을 마음대로 바꾸기 어렵습니다. (촌스러울 수 있음)
Lucide 아이콘: SVG(벡터) 방식이라서,
크기를 키워도 절대 깨지지 않습니다.
글자 색상(text-red-500)을 그대로 따라갑니다. (색 변경 자유자재)
모든 운영체제에서 똑같이 깔끔하고 세련된 디자인을 보여줍니다.
현업에서 가장 많이 쓰는 모던한 아이콘 라이브러리 중 하나입니다.
3. 로컬 파일에 정의해야 하나요? (index.css 처럼?)
아니요, 전혀 필요 없습니다! 🙅‍♂️ 이미 
package.json
에 lucide-react가 설치되어 있기 때문에(방금 확인했습니다!), 남이 만들어둔 걸 **가져다 쓰기(import)**만 하면 됩니다. 내가 직접 아이콘을 그리거나 CSS로 정의할 필요가 전혀 없어요.

사용 예시:

tsx
import { Heart } from 'lucide-react';
// 그냥 컴포넌트처럼 쓰면 하트 아이콘이 뿅 나옵니다!
<Heart className="text-red-500 w-6 h-6" />
아주 편리하죠? 😊






## front/feature-56 관련 (스터디 룸 생성하기)
2026. 01. 30.
확인해본 결과, 현재 백엔드에는 '해시태그'를 저장할 공간이 없습니다. 🙅‍♂️

Study.java
 엔티티를 분석해보니, 현재 존재하는 필드는 다음과 같습니다.

- Keywords keywords: (Enum) 정해진 주제 중 하나만 선택 가능 (단일 값)
- String title: 스터디 제목
- String description: 스터디 설명 (TEXT)
즉, 사용자가 자유롭게 입력하는 '해시태그(#)'를 별도로 저장하는 컬럼은 없습니다.

**지금 할 수 있는 방법**:

1. **백엔드 개발자에게 요청**: "Study 엔티티에 List<String> hashtags 혹은 Set<String> hashtags 컬럼을 만들어주세요!"라고 요청해야 합니다. (이게 정석입니다.)
2. **임시방편 (비추천)**: description에 텍스트로 같이 저장하는 방법이 있지만, 나중에 태그로 검색하거나 필터링하기 어렵습니다.
👉 **결론**: 백엔드 개발자분께 **"스터디 생성 시 사용자가 입력한 커스텀 해시태그를 저장할 DB 컬럼이 필요합니다"**라고 전달해주셔야 할 것 같습니다! 제가 찾은 파일 위치는 backend/.../domain/study/entity/Study.java입니다.


2026. 01. 30.
1. common/Modal.tsx의 재사용성
현재 만든 모달은 "껍데기(Container)" 역할만 합니다. 즉, 모달이 떴을 때 **"그 안에 무엇을 보여줄지"**는 모달을 쓰는 쪽(부모 컴포넌트)에서 결정합니다.

- children: 모달의 본문 내용 (텍스트, 이미지, 폼 등 무엇이든 가능)
- footer: 하단 버튼 (확인/취소, 저장/닫기 등 상황에 맞게 변경 가능)
- isOpen / onClose: 모달이 열려있는지, 닫아야 하는지를 제어
  이렇게 만들면, 나중에 "로그인 모달", "알림 모달", "이미지 미리보기 모달" 등을 만들 때 또 CSS를 작성할 필요 없이 <Modal> ... </Modal>만 가져다 쓰면 되므로 재사용성이 매우 높습니다.

2. 버튼 위치 변경
[강퇴하기] 버튼을 왼쪽, [취소] 버튼을 오른쪽으로 순서를 변경했습니다.

3. "다시 참여할 수 없는" 로직 (Ban Logic)
백엔드 엔티티를 확인해본 결과, 구현이 가능합니다!

- 백엔드 현황: StudyMember 엔티티에 status 필드가 있고, StudyMemberStatus Enum에 KICKED("강퇴") 상태가 이미 정의되어 있습니다.
- 구현 로직:
  1. 강퇴 시: 해당 멤버의 레코드를 삭제(DELETE)하는 것이 아니라, status를 KICKED로 수정(UPDATE)합니다.
  2. 재가입 시: 백엔드에서 가입 요청이 들어왔을 때, 해당 스터디에 user_id로 조회된 기록이 있는지 확인합니다.
  3. 차단: 조회된 기록의 상태가 KICKED라면, "강퇴된 멤버입니다"라는 에러를 반환하여 가입을 막습니다.
- 참고: StudyMember 테이블에는 (user_id, study_id)가 유니크 키(Unique Constraint)로 잡혀 있어, 한 번 강퇴된 유저가 중복으로 레코드를 생성할 수 없는 구조라 데이터 무결성도 지켜집니다.

이제 백엔드 개발자에게 **"강퇴 API 구현할 때 레코드 삭제하지 말고 status만 KICKED로 바꿔주세요!"**라고 요청하시면 됩니다.


2026. 01. 30.
1. 스터디장 권한 제어 구현 완료
- 기능 구현:
  - 스터디 상세 페이지: 로그인한 유저가 해당 스터디의 **방장(LEADER)**일 때만 우측 상단의 [설정] 톱니바퀴 아이콘이 보입니다.
  - 스터디 설정 페이지: URL로 직접 접근하더라도, 방장이 아니라면 "접근 권한이 없습니다"라는 메시지와 함께 강제로 페이지에서 튕겨 나가도록(Redirect) 안전장치를 마련해두었습니다. (현재는 Mock 데이터 환경이라 주석 처리 해두었지만, 로직은 완성되어 있습니다.)
- 구현 원리:
  - useAuthStore에 저장된 내 정보(userInfo.id)와 스터디 멤버 리스트(study.members)를 비교합니다.
  - 멤버 리스트에서 '나'를 찾고, 내 역할(role)이 'LEADER'인지 확인합니다.

2. 백엔드 엔티티 관련 답변
네, 백엔드 엔티티를 완벽하게 활용할 수 있습니다. 이미 필요한 구조가 다 갖춰져 있습니다.

  - StudyMember 엔티티:
    - role 필드 (StudyMemberRole Enum): LEADER, MEMBER 값이 있어 누가 방장인지 구분할 수 있습니다.
    - user 필드: 해당 멤버가 어떤 유저인지 식별할 수 있습니다.
    - study 필드: 어떤 스터디에 속해있는지 알 수 있습니다.
따라서 백엔드 개발자에게 별도로 요청할 것은 없으며, 프론트엔드에서 현재 로그인한 유저의 ID와 스터디 멤버 정보를 비교하는 로직만 잘 연동하면 됩니다.









현재 프로젝트에는 예쁜 달력을 만드는 데 필요한 라이브러리가 없습니다. (<input type="date">는 브라우저 기본 못생긴 달력만 나옵니다.)

보내주신 사진처럼 **"커스텀 가능한 예쁜 달력"**을 만드려면 보통 react-day-picker 와 date-fns 라는 라이브러리를 많이 사용합니다. (Shadcn UI도 내부적으로 이것을 씁니다.)

이 라이브러리를 설치해서, 사진과 똑같이 깔끔하고 현대적인 **커스텀 달력(Calendar Component)**을 만들어드릴까요?

(설치 명령어: npm install react-day-picker date-fns)







요청하신 대로 세련된 "시간 선택 모달(Wheel Picker)"을 구현했습니다!

디자인: 보내주신 사진과 똑같이 iOS 스타일의 휠(Wheel) 방식을 적용했습니다. 위아래로 드르륵 굴려서 시간을 선택할 수 있습니다.
기능:
embla-carousel 라이브러리를 설치하여 부드러운 스크롤 손맛을 살렸습니다.
5분 단위로 시간을 선택하도록 설정했습니다. (00분, 05분, 10분...)
오전/오후(AM/PM) 방식이지만, 내부적으로는 24시간 포맷으로 저장되어 백엔드와 문제없이 연동됩니다.
이제 [시작 시간]과 [종료 시간]을 눌러보시면, 훨씬 고급스러운 시간 선택창이 뜰 것입니다! 확인해 보세요.


2026. 02. 02.
📝 추천 메시지
"스터디 생성 페이지 관련 질문입니다.

현재 UI에서는 스터디 주기를 "n주에 n회" 형식으로 설정할 수 있도록 구현했는데요, 백엔드 
Study
 엔티티를 보니 period (주 N회) 필드 하나만 있는 것 같습니다.

혹시 다음 중 어떤 방향으로 진행하면 될까요?

1. 프론트에서 "주 N회" 형식 하나로 단순화
2. 백엔드에 periodWeeks + periodTimes 필드 추가

기획 의도에 맞는 방향으로 알려주시면 맞춰서 수정하겠습니다!"**


## front/feature-75 관련 (찜한 스터디)
2026. 02. 02.
**✅ 찜한 스터디 기능 구현 완료!**
**생성된 파일**
- `useLikedStudiesStore.ts`:	Zustand 전역 상태 + localStorage 자동 저장
- `LikedStudiesPage.tsx`:	찜한 스터디 목록 페이지 + 빈 상태 UI

**수정된 파일**
- `HomePage.tsx`:	로컬 상태 → 전역 store 사용
- `App.tsx`: 라우트 /study/liked 추가

**백엔드 연동 준비**
코드에 TODO 주석이 있으며, 백엔드 API 완성 시 연동 가능합니다:
- POST /api/v1/studies/{studyId}/like
- DELETE /api/v1/studies/{studyId}/like

**현재 동작 vs 원하는 동작**
- **현재**: 하트 클릭 → 즉시 카드 사라짐
- **원하는 동작**: 하트 클릭 → 빈 하트로 변경 (카드 유지) → 새로고침 → 그때 카드 제거
- **구현 방법**: 페이지 로드 시점의 찜 목록을 "스냅샷"으로 저장하고, 하트 토글은 전역 store만 업데이트합니다.
```
// 1. 페이지 로드 시 현재 찜 목록을 "스냅샷"으로 저장
const [initialLikedIds, setInitialLikedIds] = useState<string[]>([]);

useEffect(() => {
    setInitialLikedIds(likedStudyIds);
}, []); // 마운트 시 1회만 실행

// 2. 스냅샷 기준으로 필터링 (하트 취소해도 카드 유지)
const likedStudies = useMemo(() => {
    return ALL_STUDIES.filter((study) => initialLikedIds.includes(study.id));
}, [initialLikedIds]);

// 3. 하트 아이콘은 실시간 store 상태(isLiked) 반영
isFavorite={isLiked(study.id)}
```



## front/feature-104 관련 (리팩토링)
2026. 02. 03.



## front/feature- 관련 (빌드 에러)
2026. 02. 03.
```빌드 에러 터미널 로그
(base) PS C:\Users\SSAFY\Desktop\SSAFY\S14P11A503> npm run build

> a503@0.0.0 build
> tsc && vite build

src/components/meetingroom/ChatSidebar.tsx:14:26 - error TS6133: 'setShowKeywords' is declared but its value is never read.

14     const [showKeywords, setShowKeywords] = useState(true)
                            ~~~~~~~~~~~~~~~

src/components/meetingroom/ParticipantList.tsx:2:46 - error TS6133: 'Crown' is declared but its value is never read.

2 import { User, Mic, MicOff, Video, VideoOff, Crown, X } from 'lucide-react'
                                               ~~~~~

src/components/meetingroom/RoomHeader.tsx:7:28 - error TS6133: 'isAudit' is declared but its value is never read.

7 export function RoomHeader({ isAudit }: RoomHeaderProps) {
                             ~~~~~~~~~~~

src/hooks/useOpenVidu.ts:3:52 - error TS6133: 'StreamManager' is declared but its value is never read.

3 import { OpenVidu, Session, Publisher, Subscriber, StreamManager, Connection } from 'openvidu-browser'
                                                     ~~~~~~~~~~~~~       

src/hooks/useOpenVidu.ts:82:53 - error TS6133: 'event' is declared but its value is never read.

82                 newPublisher.once('accessAllowed', (event) => {       
                                                       ~~~~~

src/pages/CreateStudyPage.tsx:68:7 - error TS2322: Type 'string' is not assignable to type 'number'.

68       period: '1',
         ~~~~~~

src/pages/CreateStudyPage.tsx:69:7 - error TS2322: Type 'string' is not assignable to type 'number'.

69       memberLimit: '4',
         ~~~~~~~~~~~

src/pages/CreateStudyPage.tsx:134:38 - error TS2345: Argument of type '(data: CreateStudyFormValues) => void' is not assignable to parameter of type 'SubmitHandler<TFieldValues>'.
  Types of parameters 'data' and 'data' are incompatible.
    Type 'TFieldValues' is not assignable to type '{ title: string; memberLimit: number; keyword: string; description: string; studyDays: string[]; isDayNegotiable: boolean; startDate: string; startTime: string; endTime: string; period: number; imageUrl?: string | undefined; endDate?: string | undefined; }'.
      Type 'FieldValues' is missing the following properties from type '{ title: string; memberLimit: number; keyword: string; description: string; studyDays: string[]; isDayNegotiable: boolean; startDate: string; startTime: string; endTime: string; period: number; imageUrl?: string | undefined; endDate?: string | undefined; }': title, memberLimit, keyword, description, and 6 more.

134         <form onSubmit={handleSubmit(onSubmit)} className="space-y-8">
                                         ~~~~~~~~

src/pages/CreateStudyPage.tsx:232:59 - error TS2769: No overload matches this call.
  Overload 1 of 4, '(value: string | number | Date): Date', gave the following error.
    Argument of type 'string | undefined' is not assignable to parameter of type 'string | number | Date'.
      Type 'undefined' is not assignable to type 'string | number | Date'.
  Overload 2 of 4, '(value: string | number): Date', gave the following error.
    Argument of type 'string | undefined' is not assignable to parameter of type 'string | number'.
      Type 'undefined' is not assignable to type 'string | number'.      

232                       {watch('endDate') ? format(new Date(watch('endDate')), 'yyyy-MM-dd') : '종료일 (선택)'}
                                                              ~~~~~~~~~~~~~~~~


src/pages/CreateStudyPage.tsx:254:58 - error TS2769: No overload matches this call.
  Overload 1 of 4, '(value: string | number | Date): Date', gave the following error.
    Argument of type 'string | undefined' is not assignable to parameter of type 'string | number | Date'.
      Type 'undefined' is not assignable to type 'string | number | Date'.
  Overload 2 of 4, '(value: string | number): Date', gave the following error.
    Argument of type 'string | undefined' is not assignable to parameter of type 'string | number'.
      Type 'undefined' is not assignable to type 'string | number'.      

254                 initialDate={watch('endDate') ? new Date(watch('endDate')) : undefined}
                                                             ~~~~~~~~~~~~~~~~


src/pages/MeetingRoomPage.tsx:43:9 - error TS6133: 'isListening' is declared but its value is never read.

43         isListening,
           ~~~~~~~~~~~

src/pages/StudyDetailPage.tsx:28:9 - error TS6133: 'isLeader' is declared but its value is never read.

28   const isLeader = study?.members.some(member =>
           ~~~~~~~~


Found 12 errors in 7 files.

Errors  Files
     1  src/components/meetingroom/ChatSidebar.tsx:14
     1  src/components/meetingroom/ParticipantList.tsx:2
     1  src/components/meetingroom/RoomHeader.tsx:7
     2  src/hooks/useOpenVidu.ts:3
     5  src/pages/CreateStudyPage.tsx:68
     1  src/pages/MeetingRoomPage.tsx:43
     1  src/pages/StudyDetailPage.tsx:28
```


**🔍 빌드 에러 분석 (12개)**
에러를 2가지 유형으로 분류해서 설명드릴게요.

---

**📌 유형 1: 사용하지 않는 변수 (TS6133) - 7개**
"선언했지만 사용하지 않는 변수" 에러

**📌 유형 2: 타입 에러 (TS2322, TS2345, TS2769) - 5개**
모두 CreateStudyPage.tsx에서 발생!

**에러 1-2: `period`와 `memberLimit` 타입 불일치**
```typescript
// ❌ 현재 코드 (문자열)
period: '1',
memberLimit: '4',

// ✅ 수정 필요 (숫자)
period: 1,
memberLimit: 4,
```
원인: `CreateStudyFormValues` 타입에서 `number`로 정의했는데 `string`을 넣음

**에러 3: handleSubmit 타입 불일치**
```typescript
<form onSubmit={handleSubmit(onSubmit)} ...>
```
원인: `useForm`의 제네릭 타입과 onSubmit 함수의 파라미터 타입이 맞지 않음

**에러 4-5: endDate가 undefined일 수 있음**
```typescript
// ❌ 현재
format(new Date(watch('endDate')), 'yyyy-MM-dd')

// ✅ 수정
watch('endDate') && format(new Date(watch('endDate')!), 'yyyy-MM-dd')
```
원인: `endDate`가 선택 필드라서 `undefined`일 수 있는데, `new Date()`에 `undefined` 전달 불가

---

**📋 빌드 에러 수정 계획**
**1️⃣ CreateStudyPage.tsx (5개 에러) - 가장 중요!**
| 줄 | 에러 | 수정 계획 |
|---|------|---------|
| 68 | `period: '1'` → 타입 불일치 | `period: 1` (문자열 → 숫자) |
| 69 | `memberLimit: '4'` → 타입 불일치 | `memberLimit: 4` (문자열 → 숫자) |
| 134 | `handleSubmit(onSubmit)` 타입 불일치 | `useForm<CreateStudyFormValues>()` 제네릭 명시 확인 |
| 232 | `watch('endDate')` undefined 가능 | `watch('endDate')!` 또는 조건부 렌더링 |
| 254 | `watch('endDate')` undefined 가능 | `watch('endDate')!` non-null assertion |


**2️⃣ 미사용 변수 정리 (7개 에러)**
| 파일 | 줄 | 미사용 변수 | 수정 계획 |
|------|---|------------|---------|
| ChatSidebar.tsx | 14 | `setShowKeywords` | `_setShowKeywords`로 prefix 추가 또는 삭제 |
| ParticipantList.tsx | 2 | `Crown` | import에서 제거 |
| RoomHeader.tsx | 7 | `isAudit` | `_isAudit`로 변경 (나중에 사용 예정이면) |
| useOpenVidu.ts | 3 | `StreamManager` | import에서 제거 |
| useOpenVidu.ts | 82 | `event` | `_event`로 변경 또는 제거 |
| MeetingRoomPage.tsx | 43 | `isListening` | `_isListening`로 변경 또는 삭제 |
| StudyDetailPage.tsx | 28 | `isLeader` | `_isLeader`로 변경 (향후 사용 예정) |


**📊 수정 요약**
| 파일 | 문제 | 해결 방법 |
|------|------|----------|
| CreateStudyPage.tsx | `period`/`memberLimit` 타입 불일치, `endDate` undefined | 숫자로 변경, non-null assertion 추가 |
| ChatSidebar.tsx | 미사용 `setShowKeywords` | useState → 상수로 변경 |
| ParticipantList.tsx | 미사용 `Crown` import | import 제거 |
| RoomHeader.tsx | 미사용 `isAudit` prop | `_props`로 변경 |
| useOpenVidu.ts | 미사용 `StreamManager`, `event` | import 제거, `_event`로 변경 |
| MeetingRoomPage.tsx | 미사용 `isListening` | destructure에서 제거 |
| StudyDetailPage.tsx | 미사용 `isLeader`, `userInfo`, `useAuthStore` | 모두 주석 처리 |